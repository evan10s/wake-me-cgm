<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="at.str.evan.wakemecgm.MainActivity">

    <TextView
        android:text="Waiting for server message..."
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="11dp"
        android:id="@+id/bg"
        android:textAppearance="@android:style/TextAppearance.Material.Large" />

    <com.github.mikephil.charting.charts.ScatterChart
        android:id="@+id/chart"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/bg"
        android:layout_above="@+id/confirm_alert" />

    <!-- android:textIsSelectable="true" is from http://stackoverflow.com/a/5938476/5434744 and prevents keyboard from appearing when input is tapped-->
    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@android:style/TextAppearance.Medium"
        android:textIsSelectable="true"
        android:cursorVisible="false"
        android:selectAllOnFocus="true"
        android:inputType="textMultiLine|textNoSuggestions|textLongMessage"
        android:id="@+id/firebaseToken"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:text="Put this value into the To: field on the server"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/getStarted"
        android:textAppearance="@android:style/TextAppearance.Material.Medium"
        android:layout_above="@+id/firebaseToken"
        android:layout_alignStart="@+id/firebaseToken" />

    <Button
        android:text="Confirm Alert"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/confirm_alert"
        android:layout_marginBottom="33dp"
        android:layout_above="@+id/firebaseToken"
        android:layout_alignStart="@+id/getStarted"
        android:layout_alignParentEnd="true" />
</RelativeLayout>
